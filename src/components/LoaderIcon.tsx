const LoaderIcon: React.FC = () => (
  <div className="flex items-center justify-center">
    <i
      className="my-10 pi pi-spin pi-spinner text-appBlue"
      style={{ fontSize: "2rem" }}
    ></i>
  </div>
);

export default LoaderIcon;
